/* Version Comments:
 1.18  egb Use global DATA.DICT if local DATA.DICT doesn't define
	   field table.
 1.19  egb Use viewio, so we can update when using a secondary key.
 1.20  egb Data dictionary field type 'T' is BT_TIME.
*/
const char btbr_version[] = "1.20";
